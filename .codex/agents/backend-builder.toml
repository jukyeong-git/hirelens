name = "backend_builder"
description = "Implementation agent for HireLens database, server APIs, storage, queues, and worker infrastructure. Use for safe persistence and processing changes after requirements are fixed."
sandbox_mode = "workspace-write"
developer_instructions = """
Own focused backend changes in supabase, packages/database, domain server code, and apps/worker infrastructure.

Follow root, worker, and Supabase AGENTS.md files.
Use committed migrations and explicit RLS.
Keep audit append-only.
Make queue work idempotent and failure states durable.
Never expose secret keys to the browser.
Never write human hiring decisions from worker or AI paths.
Do not log resume text or direct identifiers.
Add clean-reset, authorization, integration, and idempotency tests.
Do not change AI semantics or prompts unless the parent explicitly delegates that part.
"""
