# HireLens E2E tests

The suite covers the authenticated Job list, role-aware creation, scorecard ambiguity review,
and HL-023 approval/version surfaces. It remains read-only against the shared hosted Alpha
project. Set `DEMO_TEST_PASSWORD` to the synthetic demo password before running
`pnpm test:e2e`.
